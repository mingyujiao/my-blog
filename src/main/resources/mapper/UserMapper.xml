<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.blog.mapper.UserMapper">

    <resultMap id="currentUserInfo" type="com.my.blog.entity.CurrentUserInfo">
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="avatar_url" jdbcType="VARCHAR" property="avatarUrl" />
    </resultMap>

    <select id="queryUserInfoByName" resultMap="currentUserInfo">
        SELECT username, avatar_url  FROM user WHERE username =  #{username}
    </select>
</mapper>
